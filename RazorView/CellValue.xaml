<UserControl x:Class="RazorView.CellValue"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <DockPanel>
        <Button Name="ExpandButton" DockPanel.Dock="Right" Height="30" Click="Button_Click" Content="???"/>
        <TextBox IsReadOnly="False" Name="TextBox" MinWidth="50" Text="{Binding Path=Value, Mode=OneWay}" FontSize="10">
            <TextBox.BorderBrush>
                <SolidColorBrush />
            </TextBox.BorderBrush>
            <TextBox.Background>
                <SolidColorBrush />
            </TextBox.Background>
        </TextBox>
    </DockPanel>
</UserControl>
